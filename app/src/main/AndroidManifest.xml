<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arpan.onlineevents">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OnlineActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".desc1" />
        <activity android:name=".desc2" />
        <activity android:name=".desc3" />
        <activity android:name=".desc4" />
        <activity android:name=".desc5" />
        <activity android:name=".desc6" />
        <activity android:name=".desc7" />
        <activity android:name=".desc8"></activity>
    </application>

</manifest>